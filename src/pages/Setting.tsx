import React, { memo } from "react";
import HeaderLayout from "../components/templates/HeaderLayout";

const Setting: React.VFC = memo(() => {
  return (
    <HeaderLayout>
      <div>設定ページです</div>
    </HeaderLayout>
  );
});

export default Setting;
